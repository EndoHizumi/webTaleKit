{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "npm",
      "script": "build",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "label": "npm: build",
      "detail": "rimraf ./dist/ && tsc && cp -r package.json engineConfig.json README.md parser/ dist/ && cp -r src/utils/ dist/src && cp src/core/index.js dist/src/core/"
    }
  ]
}
